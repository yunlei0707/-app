# 包管理器通用配置
registry=https://registry.npmjs.org
# 开启并行下载
npm-parallel=true
# 开启缓存
cache=~/.npm-cache
# 自动修复peer依赖冲突
auto-install-peers=true
# 忽略peer依赖警告
legacy-peer-deps=true
# 启用lockfile（pnpm会使用pnpm-lock.yaml）
package-lock=true
# 静默安装，减少日志
loglevel=warn
# 严格SSL
strict-ssl=false
